server:
  port: ${PORT:9090}
  address: 0.0.0.0

spring:
  datasource:
    url: jdbc:postgresql://dpg-d2s1p9nfte5s739ftvp0-a:5432/jyoti_software_services
    username: jyoti_software_services_user
    password: 7ndmr2HzMdAaGmMAlmmERn81TzEBOWlw
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

  mvc:
    static-path-pattern: /**
    view:
      prefix: /WEB-INF/views/

  thymeleaf:
    cache: false
    mode: HTML
    prefix: classpath:/templates/
    suffix: .html

  web:
    resources:
      static-locations: classpath:/static/

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
